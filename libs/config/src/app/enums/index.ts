export enum AppConfigEnum {
  APP_CONFIGURATION_TOKEN = 'application',
  APP_ENV = 'APP_ENV',
  APP_PORT = 'APP_PORT',
  ENV_FILE_PATH = '.env.app-config.local',
  APP_CONFIGURATION_SERVICE = 'AppConfigurationService',
}

export enum AppConfigEnvVariablesPathEnum {
  APP_ENV = 'application.env',
  APP_PORT = 'application.port',
}
